server:
  port: ${PORT:10000}

spring:
  datasource:
    url: jdbc:mysql://mysql-2f1ec983-harry990722.i.aivencloud.com:23766/defaultdb?ssl-mode=REQUIRED
    username: avnadmin
    password: AVNS_kEZP2NjYYFIxuCMolrM
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:

logging:
  level:
    com.zaxxer.hikari: DEBUG

# ⭐ JWT 設定（正式環境建議從環境變數讀取）
jwt:
  secret: ${JWT_SECRET:MySecretKey_MustBe32CharsOrMore!!}
  expiration: 604800000  # 7天（毫秒）
