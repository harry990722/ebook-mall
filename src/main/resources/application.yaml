server:
  port: ${PORT:10000}

spring:
  datasource:
    url: jdbc:mysql://mysql-2f1ec983-harry990722.i.aivencloud.com:23766/defaultdb?ssl-mode=REQUIRED
    username: avnadmin
    password: AAVNS_kEZP2NjYYFIxuCMolrM
    driver-class-name: com.mysql.cj.jdbc.Driver

  # 修正：jpa 必須在 spring 的下一層
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    # 增加這行可以確保 Hibernate 使用正確的 MySQL 語法
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

logging:
  level:
    com.zaxxer.hikari: DEBUG
